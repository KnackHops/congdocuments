import SectionWrapper from "../../../wrappers/SectionWrapper"

const Register = () => {
    return (
        <SectionWrapper sectionClass={ "register" }>
            <h1> Register </h1>
        </SectionWrapper>
     )
}

export default Register;